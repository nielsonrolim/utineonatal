<%- model_class = Visit -%>
<div class="page-header">
  <h1>Histórico de Visitas por Leito</h1>
  <h3>Leito <%= @bed.id %></h3>
</div>
<%= link_to('javascript:history.back()', :class => 'btn btn-default') do %>
    <span class="glyphicon glyphicon-chevron-left"></span> Voltar
<% end %>
<br /><br />
<table class="table table-striped">
  <thead>
  <tr>
    <th class="col-xs-1">Cód.</th>
    <th class="col-xs-2 col-sm-2">Data da Visita</th>
    <th class="col-xs-8 col-sm-4">Paciente</th>
    <th class="col-sm-4 hidden-xs">Responsável</th>
    <th class="acoes col-xs-1"><%= t '.actions', :default => t("helpers.actions") %></th>
  </tr>
  </thead>
  <tbody>
  <% @visits.each do |visit| %>
      <tr>
        <td><%= link_to visit.id, "#" %></td>
        <td><%= visit.datahora.strftime("%d/%m/%Y - %H:%M") %></td>
        <td><%= (visit.hospitalization.patient.nil?) ? "Paciente apagado" : visit.hospitalization.patient.name %></td>
        <td class="hidden-xs"><%= (visit.user.nil?) ? "Usuário apagado" : visit.user.name %></td>
        <td class="acoes">
          <%= link_to('Detalhes', visit_details_path(visit), :class => 'btn btn-default ') %>
          <%= link_to('Imprimir', visit_details_pdf_path(visit), :target => '_blank', :class => 'btn btn-default ') %>
        </td>
      </tr>
  <% end %>
  </tbody>
</table>

<%= link_to('javascript:history.back()', :class => 'btn btn-default') do %>
    <span class="glyphicon glyphicon-chevron-left"></span> Voltar
<% end %>
